# 교육자료 샘플관 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 제작 지침과 분리된 `/sample/` 경로에 건조시럽 계산기의 첫 결과와 개선 결과를 독립 실행형 샘플로 배포한다.

**Architecture:** `/sample/index.html`은 교육자료 목록과 두 결과 링크만 담당한다. 두 원본 HTML은 로직을 수정하지 않고 `/sample/dry/first/index.html`과 `/sample/dry/final/index.html`로 복제하며, `/dry/index.html`은 변경하지 않는다.

**Tech Stack:** 정적 HTML, GitHub Pages, SHA-256, curl

---

### Task 1: 샘플 원본 고정 및 배치

**Files:**
- Create: `sample/dry/first/index.html`
- Create: `sample/dry/final/index.html`
- Verify unchanged: `dry/index.html`

- [ ] **Step 1: 배포 전 해시 기록**

Run:

```bash
shasum -a 256 dry/index.html \
  '/Users/maicto/Downloads/건조시럽_환산계수_계산기.html' \
  '/Users/maicto/Downloads/dry-syrup-calculator (3).html'
```

Expected: 세 파일의 SHA-256이 각각 한 줄씩 출력된다.

- [ ] **Step 2: 공개 폴더 생성**

Run:

```bash
mkdir -p sample/dry/first sample/dry/final
```

Expected: 두 경로가 생성되고 오류가 없다.

- [ ] **Step 3: 원본 HTML 복제**

Run:

```bash
cp '/Users/maicto/Downloads/건조시럽_환산계수_계산기.html' sample/dry/first/index.html
cp '/Users/maicto/Downloads/dry-syrup-calculator (3).html' sample/dry/final/index.html
```

Expected: 두 `index.html`이 생성된다.

- [ ] **Step 4: 복제 무결성 검증**

Run:

```bash
cmp '/Users/maicto/Downloads/건조시럽_환산계수_계산기.html' sample/dry/first/index.html
cmp '/Users/maicto/Downloads/dry-syrup-calculator (3).html' sample/dry/final/index.html
```

Expected: 출력 없이 종료 코드 0.

### Task 2: 샘플 목록 페이지 작성

**Files:**
- Create: `sample/index.html`

- [ ] **Step 1: 목록 페이지 생성**

Create `sample/index.html` with this complete structure:

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>교육자료 샘플 | 마이팜</title>
  <style>
    :root { color-scheme: light; font-family: system-ui, sans-serif; }
    body { max-width: 760px; margin: 0 auto; padding: 48px 20px; color: #172033; background: #f5f7fb; }
    h1 { margin-bottom: 8px; }
    .intro { margin-top: 0; color: #526078; }
    section { margin-top: 32px; padding: 24px; border: 1px solid #dce2ec; border-radius: 16px; background: #fff; }
    .links { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 12px; margin-top: 20px; }
    a { display: block; padding: 16px; border-radius: 12px; color: #fff; background: #275efe; text-decoration: none; font-weight: 700; }
    a.secondary { color: #1f2b44; background: #e8edf6; }
    small { display: block; margin-top: 6px; font-weight: 400; opacity: .82; }
  </style>
</head>
<body>
  <main>
    <h1>교육자료 샘플</h1>
    <p class="intro">제작 전후의 차이를 직접 확인하는 교육용 결과물입니다.</p>
    <section>
      <h2>건조시럽 계산기</h2>
      <p>짧은 요청으로 만든 첫 결과와, 약사가 명세와 검증을 더한 결과를 비교합니다.</p>
      <div class="links">
        <a class="secondary" href="./dry/first/">첫 결과 열기<small>최소 입력으로 생성한 결과</small></a>
        <a href="./dry/final/">개선 결과 열기<small>지침과 검증을 반영한 결과</small></a>
      </div>
    </section>
  </main>
</body>
</html>
```

- [ ] **Step 2: 링크와 문구 정적 검증**

Run:

```bash
rg -n 'dry/first|dry/final|교육자료 샘플|제작 전후' sample/index.html
```

Expected: 두 링크와 제목·설명 문구가 모두 출력된다.

### Task 3: 무변경 검증, 배포 및 운영 확인

**Files:**
- Verify: `dry/index.html`
- Verify: `sample/index.html`
- Verify: `sample/dry/first/index.html`
- Verify: `sample/dry/final/index.html`

- [ ] **Step 1: `/dry/` 무변경과 저장소 상태 확인**

Run:

```bash
git diff --exit-code -- dry/index.html
git diff --check
git status --short
```

Expected: 첫 두 명령은 출력 없이 성공하고, 상태에는 `sample/`과 계획 문서만 나타난다.

- [ ] **Step 2: 샘플 구현 커밋**

Run:

```bash
git add sample docs/superpowers/plans/2026-08-11-교육자료-샘플관.md
git commit -m '교육자료 샘플관 추가'
```

Expected: 새 커밋에 목록 페이지와 두 샘플이 포함된다.

- [ ] **Step 3: GitHub Pages 원격에 배포**

Run:

```bash
git push origin main
```

Expected: `main` 브랜치 push가 성공한다.

- [ ] **Step 4: HTTPS 운영 경로 확인**

Run:

```bash
for url in \
  https://edu.maipharm.com/dry/ \
  https://edu.maipharm.com/sample/ \
  https://edu.maipharm.com/sample/dry/first/ \
  https://edu.maipharm.com/sample/dry/final/
do
  curl -L --retry 6 --retry-delay 5 -o /dev/null -s -w '%{http_code} %{url_effective}\n' "$url"
done
```

Expected: 네 URL 모두 최종 HTTPS URL과 HTTP 200을 출력한다.

- [ ] **Step 5: 운영 파일 무결성 확인**

Run:

```bash
curl -fsSL https://edu.maipharm.com/dry/ | shasum -a 256
curl -fsSL https://edu.maipharm.com/sample/dry/first/ | shasum -a 256
curl -fsSL https://edu.maipharm.com/sample/dry/final/ | shasum -a 256
```

Expected: `/dry/` 해시는 Task 1의 기존 해시와 같고 두 샘플 해시는 각각 다운로드 원본 해시와 같다.

